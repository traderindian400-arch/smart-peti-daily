import React from 'react';
import { NavLink } from 'react-router-dom';
import { useApp } from '../../hooks/useApp';
import ThemeToggle from '../ui/ThemeToggle';
import LanguageSelector from '../ui/LanguageSelector';

const navItems = [
  { to: '/', label: 'home', icon: '🏠' },
  { to: '/chat', label: 'chat', icon: '💬' },
  { to: '/app', label: 'appBuilder', icon: '📱' },
  { to: '/website', label: 'websiteBuilder', icon: '🌐' },
  { to: '/ebook', label: 'ebook', icon: '📘' },
  { to: '/game', label: 'gameBuilder', icon: '🎮' },
  { to: '/projects', label: 'projects', icon: '📂' },
  { to: '/settings', label: 'settings', icon: '⚙️' },
];

export default function Sidebar() {
  const { user, logout, t } = useApp();
  return (
    <aside className="hidden md:flex md:flex-col w-[220px] h-screen fixed left-0 top-0 bg-bg-secondary border-r border-border p-4 z-50">
      <div className="flex items-center gap-2 mb-6 px-2">
        <span className="text-xl font-extrabold bg-gradient-to-r from-accent to-accent-light bg-clip-text text-transparent">SmartPeti</span>
        <span className="text-[10px] font-semibold bg-accent/20 text-accent px-2 py-0.5 rounded-full">AI</span>
      </div>
      <nav className="flex-1 space-y-1">
        {navItems.map(item => (
          <NavLink
            key={item.to}
            to={item.to}
            className={({ isActive }) =>
              `flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium transition-all ${
                isActive ? 'bg-accent/10 text-accent' : 'text-text-muted hover:text-text-primary hover:bg-bg-input'
              }`
            }
          >
            <span>{item.icon}</span>
            {t(item.label)}
          </NavLink>
        ))}
      </nav>
      <div className="mt-auto pt-4 border-t border-border space-y-3">
        {user && (
          <div className="flex items-center gap-2 px-3 py-2 text-sm">
            <span className="w-5 h-5 rounded-full bg-accent/20 flex items-center justify-center text-accent">👤</span>
            <span className="truncate">{user.name || user.email}</span>
          </div>
        )}
        <div className="flex items-center gap-2 px-3">
          <ThemeToggle />
          <LanguageSelector />
          {user && (
            <button
              onClick={logout}
              className="text-sm text-red-400 hover:text-red-300 transition ml-auto"
            >
              {t('logout')}
            </button>
          )}
        </div>
      </div>
    </aside>
  );
}

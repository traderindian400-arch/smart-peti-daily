import React from 'react';

export const Input = ({ className = '', ...props }) => (
  <input
    className={`w-full bg-bg-input border border-border rounded-xl px-4 py-3 text-text-primary placeholder-text-muted focus:border-accent focus:ring-2 focus:ring-accent/30 outline-none transition ${className}`}
    {...props}
  />
);

export const Textarea = ({ className = '', ...props }) => (
  <textarea
    className={`w-full bg-bg-input border border-border rounded-xl px-4 py-3 text-text-primary placeholder-text-muted focus:border-accent focus:ring-2 focus:ring-accent/30 outline-none transition resize-y min-h-[80px] ${className}`}
    {...props}
  />
);
